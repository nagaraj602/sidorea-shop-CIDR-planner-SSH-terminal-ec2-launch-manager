# Spin up your Docker container using:

```
docker run -d -p 3000:3000 your-image, then execute this script
```

---
## To have SSL on the docker container, run this script:

```
bash SSL-install-on-docker-container-script.sh
```
