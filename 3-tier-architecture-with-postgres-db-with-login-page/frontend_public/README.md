# For Docker container, run this command:
```
docker run -d -p 8080:80 my-frontend-image
```

---
## Once both frontend and backend app container is working, you can run the shell script to install SSL:

```
bash SSL-install-docker-container-script.sh
```
